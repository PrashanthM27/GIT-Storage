({
    handleActive:function(cmp,event,helper){
        var action=event.getSource().get("v.name");
        switch(tab.get('v.Id')){
            case 'tab11':
                break;
            case 'tab22':
                break;
        }
        var meuItem=event.detail.menuItem;
        var itemValue=menuItem.get("v.value");
        var itemValue=event.getParam("value");
    }
})